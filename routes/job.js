//Paket
const express = require("express");
const db = require("../db");

const route = express.Router();

//Routes
route.get("/", (req, res) => {

})

route.get("/:id", (req, res) => {

})

module.exports = route;
